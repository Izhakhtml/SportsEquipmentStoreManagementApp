
@{
    ViewBag.Title = "Pants";
}
<body style="background: rgb(214, 206, 206)">
    <h1 style="border-bottom:2px dotted black;border-bottom-width:5px;text-align:center;font-family:'Lucida Sans', 'Lucida Sans Regular', 'Lucida Grande', 'Lucida Sans Unicode', Geneva, Verdana, sans-serif;font-size:50px">pants</h1>
    <div style="display:flex;flex-wrap:wrap;justify-content:center">

        @using (Html.BeginForm("Pants", "SportShop"))
        {
            <select name="value" style="padding:10px;border-radius:5px;">
                <option style="display:none">Sort by:</option>
                <option value="1">PRICE</option>
                <option value="2">AMOUNT</option>
            </select>
            <input type="submit" value="Sort" style="margin:15px;padding:10px;border-radius:10px;" />
        }
        @using (Html.BeginForm("Pants", "SportShop"))
        {
          <select name="value" style="padding:10px;border-radius:5px;">
                <option style="display:none">Filtering by:</option>
                <option value="3">Short Pants</option>
                <option value="4">Long Pants</option>
                <option value="5">Drifit Pants</option>
             </select>
            <input type="submit" value="Filter" style="margin:15px;padding:10px;border-radius:10px;" />
        }
    </div>
        <div style="display:flex;flex-wrap:wrap;justify-content:space-between;">
            @{
                foreach (var item in Model)
                {
                    <article>
                        <h2 style="text-align:center;"> @item.Company </h2>

                        <img style="width:350px;height:300px;" src="@item.LinkToImage" />

                        <h3> Shoe Type: @item.TypeOfGarment </h3>

                        <h3> Price: @item.Price </h3>

                    </article>
                }
            }
        </div>
        <div style="display:flex;flex-wrap:wrap;justify-content:center;">
            @using (@Html.BeginForm("ViewManagerPants", "SportShop"))
            {
                <button style="margin:15px;padding:10px;border-radius:10px;"> View Manager Pants</button>
            }
            @using (@Html.BeginForm("OnlyLongPants", "SportShop"))
            {
                <button style="margin:15px;padding:10px;border-radius:10px;">Long Pants</button>
            }
            @using (@Html.BeginForm("OnlyShortPants", "SportShop"))
            {
                <button style="margin:15px;padding:10px;border-radius:10px;">Short Pants</button>
            }
            @using (@Html.BeginForm("OnlytDrifitPants", "SportShop"))
            {
                <button style="margin:15px;padding:10px;border-radius:10px;">Drifit</button>
            }
        </div>
</body>